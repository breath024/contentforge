# 작업 유의사항 — 또 밟을 지뢰

작업 시작 전에 읽는다. 형식: 증상 → 원인 → 해법 → 언제 당했나.
(렌더·경로 함정 중 오래된 것은 HANDOFF 「하드런 함정」에 있다.)

## 환경·도구

- **한글 윈도우 명령 출력은 cp949** — `netstat` 등 시스템 명령을 `subprocess.run(text=True)` 로 받으면
  `PYTHONUTF8=1` 환경에서 UTF-8 로 풀다 `UnicodeDecodeError`. → 바이트로 받고 필요한 만큼만
  디코드(`decode("ascii","replace")` 등). 콘솔 출력 cp949 문제(HANDOFF)와 같은 뿌리.
  *(2026-09-11 런처 `port_pid()`)*
- **qwen3 가 `format: json` 에서 가끔 곧바로 `{}` 를 낸다** — 0.4초 만에, 에러 없이. 같은 프롬프트를
  다시 물으면 대개 정상. → LLM JSON 을 받는 곳은 빈 결과를 실패로 보고 재시도(`make_cards` 는 3회).
  스트리밍 여부와 무관(A/B 로 확인). *(2026-09-11)*
- **테스트로 서버를 띄우면 끝나고 8770 을 확인** — 테스트 스크립트가 중간에 죽으면 띄운 `app.py` 가 남아
  다음 실행이 포트 충돌. `netstat -ano -p TCP | findstr :8770` → `taskkill /PID <pid> /T /F`. *(2026-09-11)*

## 카드 렌더

- **한글 줄바꿈엔 `word-break: keep-all`** — 없으면 "써보실래/요?"처럼 글자 중간에서 끊긴다.
  새 레이아웃·템플릿을 만들 때 텍스트 블록마다 넣을 것. *(2026-09-11)*
- **사진 위 작은 글자는 테마색 금지** — accent·fg 색은 사진 명암에 따라 묻힌다. 흰색 + 그림자.
  *(2026-09-11, 표지 뱃지·페이지 번호)*

## 카피·사진을 대외용으로 낼 때

- **로컬 LLM 카피를 그대로 내보내지 말 것** — 모델은 제품을 모른다. 설명을 tone 에 넣어도 장수가 틀리고
  핵심 문구(모집·신청)가 빠진다. 제품 사실은 그 제품 문서를 근거로 사람이 다시 쓰고, 문서에 없는
  숫자(인원·기간·금액)는 지어내지 말고 비워서 물어본다. *(2026-09-11 팀 모집 카드뉴스)*
- **배경 사진 라이선스를 확인** — Openverse `license_type=commercial` 에는 CC BY(출처 표기 의무)가 섞인다.
  남에게 넘길 결과물은 `cc0`/`pdm` 만 쓰고 출처 파일을 같이 둔다. Openverse 원본은 960px 짜리가 많아
  가로 사진을 풀블리드에 깔면 흐리다 → 풀블리드(표지·CTA)엔 세로 사진. *(2026-09-11)*

## 저장소

- **공개 저장소다(breath024/contentforge)** — 결과물 폴더 이름에 비공개 제품명이 들어가면 `.gitignore` 에
  적는 것만으로 샌다(`.gitignore` 는 커밋된다). → 결과물 무시 규칙은 **`.git/info/exclude`**(로컬 전용).
  결과물 폴더·zip 은 커밋 금지. *(2026-09-11)*
